version: '3.8'

services:
  marl:
    image: docker.io/zoeyuchao/marl:latest
    container_name: marl
    hostname: marl
    ports:
      - "${MARL_PORT:-8080}:8080"
    volumes:
      - marl_data:/data
    restart: unless-stopped

volumes:
  marl_data:
    name: marl_data
