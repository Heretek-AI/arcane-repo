version: '3.8'

services:
  ntfy:
    image: docker.io/binwiederhier/ntfy:latest
    container_name: ntfy
    hostname: ntfy
    ports:
      - "${NTFY_PORT:-80}:80"
    volumes:
      - ntfy_data:/data
    restart: unless-stopped

volumes:
  ntfy_data:
    name: ntfy_data
