version: '3.8'

services:
  cockpit:
    image: docker.io/agentejo/cockpit:latest
    container_name: cockpit
    hostname: cockpit
    ports:
      - "${COCKPIT_PORT:-8080}:8080"
    volumes:
      - cockpit_data:/data
    restart: unless-stopped

volumes:
  cockpit_data:
    name: cockpit_data
