version: '3.8'

services:
  pluxml:
    image: docker.io/dujunio/pluxml:latest
    container_name: pluxml
    hostname: pluxml
    ports:
      - "${PLUXML_PORT:-8080}:8080"
    volumes:
      - pluxml_data:/data
    restart: unless-stopped

volumes:
  pluxml_data:
    name: pluxml_data
