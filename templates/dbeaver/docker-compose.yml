version: "3.8"

services:
  dbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: dbeaver
    hostname: dbeaver
    ports:
      - "${DBEAVER_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - DBEAVER_PORT=${DBEAVER_PORT:-8080}

