version: "3.8"

services:
  scrapling:
    image: python:3.12-slim
    container_name: scrapling
    hostname: scrapling
    ports:
      - "${SCRAPLING_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - SCRAPLING_PORT=${SCRAPLING_PORT:-8080}

