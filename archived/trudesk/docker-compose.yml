version: '3.8'

services:
  trudesk:
    image: docker.io/polonel/trudesk:latest
    container_name: trudesk
    hostname: trudesk
    ports:
      - "${TRUDESK_PORT:-8080}:8080"
    volumes:
      - trudesk_data:/data
    restart: unless-stopped

volumes:
  trudesk_data:
    name: trudesk_data
