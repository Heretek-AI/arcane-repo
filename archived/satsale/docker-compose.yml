version: '3.8'

services:
  satsale:
    image: docker.io/satsale/satsale:latest
    container_name: satsale
    hostname: satsale
    ports:
      - "${SATSALE_PORT:-8080}:8080"
    volumes:
      - satsale_data:/data
    restart: unless-stopped

volumes:
  satsale_data:
    name: satsale_data
