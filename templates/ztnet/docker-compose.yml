version: '3.8'

services:
  ztnet:
    image: docker.io/sinamics/ztnet:latest
    container_name: ztnet
    hostname: ztnet
    ports:
      - "${ZTNET_PORT:-8080}:8080"
    volumes:
      - ztnet_data:/data
    restart: unless-stopped

volumes:
  ztnet_data:
    name: ztnet_data
