version: '3.8'

services:
  kiwiirc:
    image: docker.io/crashbuggy/kiwiirc:latest
    container_name: kiwiirc
    hostname: kiwiirc
    ports:
      - "${KIWIIRC_PORT:-8080}:8080"
    volumes:
      - kiwiirc_data:/data
    restart: unless-stopped

volumes:
  kiwiirc_data:
    name: kiwiirc_data
