version: '3.8'

services:
  baserow:
    image: docker.io/baserow/baserow:latest
    container_name: baserow
    hostname: baserow
    ports:
      - "${BASEROW_PORT:-80}:80"
    volumes:
      - baserow_data:/data
    restart: unless-stopped

volumes:
  baserow_data:
    name: baserow_data
