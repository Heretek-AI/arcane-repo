version: "3.8"

services:
  pentaigi:
    image: vxcontrol/pentaigi:latest
    container_name: pentaigi
    hostname: pentaigi
    ports:
      - "${PENTAGI_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - PENTAGI_PORT=${PENTAGI_PORT:-8080}

