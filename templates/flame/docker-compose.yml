version: '3.8'

services:
  flame:
    image: docker.io/pawelmalak/flame:latest
    container_name: flame
    hostname: flame
    ports:
      - "${FLAME_PORT:-8080}:8080"
    volumes:
      - flame_data:/data
    restart: unless-stopped

volumes:
  flame_data:
    name: flame_data
