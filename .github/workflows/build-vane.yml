name: Build Vane

on:
  push:
    paths:
      - 'scripts/dockerfiles/vane/**'
      - 'templates/vane/**'
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Vane
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/vane:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/vane/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
