# Host port for the Conduit Matrix homeserver
MATRIX_PORT=8448

# Server name / domain for this homeserver (e.g., matrix.example.com)
MATRIX_SERVER_NAME=localhost

# Allow new user registration (set to false after initial setup)
MATRIX_ALLOW_REGISTRATION=true

# Allow federation with other Matrix servers
MATRIX_ALLOW_FEDERATION=true
