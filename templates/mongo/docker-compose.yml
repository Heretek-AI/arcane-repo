version: '3.8'

services:
  mongo:
    image: docker.io/circleci/mongo:latest
    container_name: mongo
    hostname: mongo
    ports:
      - "${MONGO_PORT:-8080}:8080"
    volumes:
      - mongo_data:/data
    restart: unless-stopped

volumes:
  mongo_data:
    name: mongo_data
