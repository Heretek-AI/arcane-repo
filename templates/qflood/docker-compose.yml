version: '3.8'

services:
  qflood:
    image: ghcr.io/engels74/qflood:latest
    container_name: qflood
    hostname: qflood
    ports:
      - "${QFLOOD_PORT:-8080}:8080"
    volumes:
      - qflood_data:/data
    restart: unless-stopped

volumes:
  qflood_data:
    name: qflood_data
