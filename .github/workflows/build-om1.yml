name: Build OM1

on:
  push:
    paths:
      - 'scripts/dockerfiles/om1/**'
      - 'templates/om1/**'
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push OM1
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/om1:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/om1/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
