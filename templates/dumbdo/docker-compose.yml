version: '3.8'

services:
  dumbdo:
    image: docker.io/dumbwareio/dumbdo:latest
    container_name: dumbdo
    hostname: dumbdo
    ports:
      - "${DUMBDO_PORT:-8080}:8080"
    volumes:
      - dumbdo_data:/data
    restart: unless-stopped

volumes:
  dumbdo_data:
    name: dumbdo_data
