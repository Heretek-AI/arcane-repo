version: '3.8'

services:
  plex:
    image: ghcr.io/linuxserver/plex:latest
    container_name: plex
    hostname: plex
    ports:
      - "${PLEX_PORT:-32400}:32400"
    volumes:
      - plex_data:/data
    restart: unless-stopped

volumes:
  plex_data:
    name: plex_data
