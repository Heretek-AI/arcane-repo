version: '3.8'

services:
  open-meteo:
    image: ghcr.io/open-meteo/open-meteo:latest
    container_name: open-meteo
    hostname: open-meteo
    ports:
      - "${OPEN_METEO_PORT:-8080}:8080"
    volumes:
      - open-meteo_data:/data
    restart: unless-stopped

volumes:
  open-meteo_data:
    name: open-meteo_data
