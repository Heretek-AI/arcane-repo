version: '3.8'

services:
  tldraw:
    image: docker.io/ratneo/tldraw:latest
    container_name: tldraw
    hostname: tldraw
    ports:
      - "${TLDRAW_PORT:-8080}:8080"
    volumes:
      - tldraw_data:/data
    restart: unless-stopped

volumes:
  tldraw_data:
    name: tldraw_data
