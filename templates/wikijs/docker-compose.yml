version: '3.8'

services:
  wikijs:
    image: ghcr.io/linuxserver/wikijs:latest
    container_name: wikijs
    hostname: wikijs
    ports:
      - "${WIKIJS_PORT:-80}:80"
    volumes:
      - wikijs_data:/data
    restart: unless-stopped

volumes:
  wikijs_data:
    name: wikijs_data
