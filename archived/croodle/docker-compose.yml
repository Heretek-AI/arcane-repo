version: '3.8'

services:
  croodle:
    image: docker.io/knex666/croodle:latest
    container_name: croodle
    hostname: croodle
    ports:
      - "${CROODLE_PORT:-8080}:8080"
    volumes:
      - croodle_data:/data
    restart: unless-stopped

volumes:
  croodle_data:
    name: croodle_data
