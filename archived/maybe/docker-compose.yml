version: '3.8'

services:
  maybe:
    image: docker.io/xcv58/maybe:latest
    container_name: maybe
    hostname: maybe
    ports:
      - "${MAYBE_PORT:-8080}:8080"
    volumes:
      - maybe_data:/data
    restart: unless-stopped

volumes:
  maybe_data:
    name: maybe_data
