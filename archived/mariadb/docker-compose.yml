version: '3.8'

services:
  mariadb:
    image: docker.io/circleci/mariadb:latest
    container_name: mariadb
    hostname: mariadb
    ports:
      - "${MARIADB_PORT:-8080}:8080"
    volumes:
      - mariadb_data:/data
    restart: unless-stopped

volumes:
  mariadb_data:
    name: mariadb_data
