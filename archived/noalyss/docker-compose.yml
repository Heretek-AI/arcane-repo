version: '3.8'

services:
  noalyss:
    image: docker.io/sleig/noalyss:latest
    container_name: noalyss
    hostname: noalyss
    ports:
      - "${NOALYSS_PORT:-8080}:8080"
    volumes:
      - noalyss_data:/data
    restart: unless-stopped

volumes:
  noalyss_data:
    name: noalyss_data
