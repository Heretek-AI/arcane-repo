version: "3.8"

services:
  deerflow:
    image: ghcr.io/bytedance/deer-flow:main
    container_name: deerflow
    hostname: deerflow
    ports:
      - "${DEER-FLOW_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - DEERFLOW_PORT=${DEERFLOW_PORT:-8080}

