version: "3.8"

services:
  beeai:
    image: i-am-bee/beeai:latest
    container_name: beeai
    hostname: beeai
    ports:
      - "${BEEAI FRAMEWORK_PORT:-8000}:8000"
    restart: unless-stopped
    environment:
      - BEEAI_PORT=${BEEAI_PORT:-8000}

