version: '3.8'

services:
  matterbridge:
    image: ghcr.io/42wim/matterbridge:latest
    container_name: matterbridge
    hostname: matterbridge
    ports:
      - "${MATTERBRIDGE_PORT:-8080}:8080"
    volumes:
      - matterbridge_data:/data
    restart: unless-stopped

volumes:
  matterbridge_data:
    name: matterbridge_data
