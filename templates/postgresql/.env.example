# -- PostgreSQL Configuration --
# Copy this file to .env and adjust values as needed.

# -- Port --
# PostgreSQL host port (default: 8080)
POSTGRESQL_PORT=8080

# -- User --
# PostgreSQL database user (default: app)
POSTGRES_USER=app
