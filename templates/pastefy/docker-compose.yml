version: '3.8'

services:
  pastefy:
    image: docker.io/interaapps/pastefy:latest
    container_name: pastefy
    hostname: pastefy
    ports:
      - "${PASTEFY_PORT:-8080}:8080"
    volumes:
      - pastefy_data:/data
    restart: unless-stopped

volumes:
  pastefy_data:
    name: pastefy_data
