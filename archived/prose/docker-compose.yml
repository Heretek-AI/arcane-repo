version: '3.8'

services:
  prose:
    image: docker.io/francois2/prose:latest
    container_name: prose
    hostname: prose
    ports:
      - "${PROSE_PORT:-8080}:8080"
    volumes:
      - prose_data:/data
    restart: unless-stopped

volumes:
  prose_data:
    name: prose_data
