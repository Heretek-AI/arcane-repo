version: '3.8'

services:
  umap:
    image: docker.io/umap/umap:latest
    container_name: umap
    hostname: umap
    ports:
      - "${UMAP_PORT:-8080}:8080"
    volumes:
      - umap_data:/data
    restart: unless-stopped

volumes:
  umap_data:
    name: umap_data
