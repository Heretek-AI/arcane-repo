version: '3.8'

services:
  romm:
    image: ghcr.io/rommapp/romm:latest
    container_name: romm
    hostname: romm
    ports:
      - "${ROMM_PORT:-8080}:8080"
    volumes:
      - romm_data:/data
    restart: unless-stopped

volumes:
  romm_data:
    name: romm_data
