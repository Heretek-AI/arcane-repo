version: '3.8'

services:
  homarr:
    image: docker.io/homarr/homarr:latest
    container_name: homarr
    hostname: homarr
    ports:
      - "${HOMARR_PORT:-8080}:8080"
    volumes:
      - homarr_data:/data
    restart: unless-stopped

volumes:
  homarr_data:
    name: homarr_data
