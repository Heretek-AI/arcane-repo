version: '3.8'

services:
  torq:
    image: docker.io/lncapital/torq:latest
    container_name: torq
    hostname: torq
    ports:
      - "${TORQ_PORT:-8080}:8080"
    volumes:
      - torq_data:/data
    restart: unless-stopped

volumes:
  torq_data:
    name: torq_data
