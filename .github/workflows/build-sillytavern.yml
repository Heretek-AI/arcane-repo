name: Build SillyTavern

on:
  push:
    paths:
      - 'scripts/dockerfiles/sillytavern/**'
      - 'templates/sillytavern/**'
  workflow_dispatch:
  schedule:
    - cron: '0 10 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push SillyTavern
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/sillytavern:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/sillytavern/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
