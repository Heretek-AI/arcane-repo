version: '3.8'

services:
  gazee:
    image: docker.io/linuxserver/gazee:latest
    container_name: gazee
    hostname: gazee
    ports:
      - "${GAZEE_PORT:-8080}:8080"
    volumes:
      - gazee_data:/data
    restart: unless-stopped

volumes:
  gazee_data:
    name: gazee_data
