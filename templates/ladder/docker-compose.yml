version: '3.8'

services:
  ladder:
    image: docker.io/themotion/ladder:latest
    container_name: ladder
    hostname: ladder
    ports:
      - "${LADDER_PORT:-8080}:8080"
    volumes:
      - ladder_data:/data
    restart: unless-stopped

volumes:
  ladder_data:
    name: ladder_data
