# Music Tag Web — placeholder template
# Music Tag Web has no Docker Hub image and no upstream Dockerfile (5.7k stars, Django+Vue).
# This template runs a minimal FastAPI server documenting the limitations.
MUSIC_TAG_WEB_PORT=8000
