version: '3.8'

services:
  ubuntu:
    image: docker.io/ubuntu:latest
    container_name: ubuntu
    hostname: ubuntu
    ports:
      - "${UBUNTU_PORT:-8080}:8080"
    volumes:
      - ubuntu_data:/data
    restart: unless-stopped

volumes:
  ubuntu_data:
    name: ubuntu_data
