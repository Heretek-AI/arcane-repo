version: '3.8'

services:
  dex:
    image: ghcr.io/dexidp/dex:latest
    container_name: dex
    hostname: dex
    ports:
      - "${DEX_PORT:-8080}:8080"
    volumes:
      - dex_data:/data
    restart: unless-stopped

volumes:
  dex_data:
    name: dex_data
