version: "3.8"

services:
  onyx:
    image: ghcr.io/arcane-repo/onyx:latest
    container_name: onyx
    hostname: onyx
    ports:
      - "${ONYX_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - ONYX_PORT=${ONYX_PORT:-8080}
