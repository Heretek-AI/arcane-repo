version: '3.8'

services:
  dokos:
    image: docker.io/numadis/dokos:latest
    container_name: dokos
    hostname: dokos
    ports:
      - "${DOKOS_PORT:-8080}:8080"
    volumes:
      - dokos_data:/data
    restart: unless-stopped

volumes:
  dokos_data:
    name: dokos_data
