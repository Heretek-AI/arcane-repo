version: '3.8'

services:
  pretix:
    image: docker.io/neumantm/pretix:latest
    container_name: pretix
    hostname: pretix
    ports:
      - "${PRETIX_PORT:-8080}:8080"
    volumes:
      - pretix_data:/data
    restart: unless-stopped

volumes:
  pretix_data:
    name: pretix_data
