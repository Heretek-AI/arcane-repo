version: '3.8'

services:
  homer:
    image: docker.io/b4bz/homer:latest
    container_name: homer
    hostname: homer
    ports:
      - "${HOMER_PORT:-8080}:8080"
    volumes:
      - homer_data:/data
    restart: unless-stopped

volumes:
  homer_data:
    name: homer_data
