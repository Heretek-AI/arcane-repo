version: '3.8'

services:
  webhook:
    image: docker.io/dialgmbh/webhook:latest
    container_name: webhook
    hostname: webhook
    ports:
      - "${WEBHOOK_PORT:-8080}:8080"
    volumes:
      - webhook_data:/data
    restart: unless-stopped

volumes:
  webhook_data:
    name: webhook_data
