version: "3.8"

services:
  hermesagent:
    image: nousresearch/hermes-agent:latest
    container_name: hermesagent
    hostname: hermesagent
    ports:
      - "${HERMES-AGENT_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - HERMES_PORT=${HERMES_PORT:-8080}

