version: '3.8'

services:
  solr:
    image: docker.io/bitnamicharts/solr:latest
    container_name: solr
    hostname: solr
    ports:
      - "${SOLR_PORT:-8080}:8080"
    volumes:
      - solr_data:/data
    restart: unless-stopped

volumes:
  solr_data:
    name: solr_data
