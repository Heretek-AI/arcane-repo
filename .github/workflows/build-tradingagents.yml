name: Build TradingAgents

on:
  push:
    paths:
      - 'scripts/dockerfiles/tradingagents/**'
      - 'templates/tradingagents/**'
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push TradingAgents
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/tradingagents:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/tradingagents/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
