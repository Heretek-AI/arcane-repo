# Docker Flare — placeholder template
# Docker Flare has no published Docker image and no upstream Dockerfile.
# This template runs a minimal FastAPI server documenting the limitations.
DOCKER_FLARE_PORT=8000
