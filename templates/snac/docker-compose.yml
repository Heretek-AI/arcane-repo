version: '3.8'

services:
  snac:
    image: docker.io/geodynamics/snac:latest
    container_name: snac
    hostname: snac
    ports:
      - "${SNAC_PORT:-8080}:8080"
    volumes:
      - snac_data:/data
    restart: unless-stopped

volumes:
  snac_data:
    name: snac_data
