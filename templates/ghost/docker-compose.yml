version: '3.8'

services:
  ghost:
    image: docker.io/library/ghost:latest
    container_name: ghost
    hostname: ghost
    ports:
      - "${GHOST_PORT:-8080}:8080"
    volumes:
      - ghost_data:/data
    restart: unless-stopped

volumes:
  ghost_data:
    name: ghost_data
