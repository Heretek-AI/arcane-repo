version: '3.8'

services:
  hotspot:
    image: docker.io/ketere/hotspot:latest
    container_name: hotspot
    hostname: hotspot
    ports:
      - "${HOTSPOT_PORT:-8080}:8080"
    volumes:
      - hotspot_data:/data
    restart: unless-stopped

volumes:
  hotspot_data:
    name: hotspot_data
