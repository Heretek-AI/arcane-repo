version: '3.8'

services:
  oscam:
    image: ghcr.io/linuxserver/oscam:latest
    container_name: oscam
    hostname: oscam
    ports:
      - "${OSCAM_PORT:-8080}:8080"
    volumes:
      - oscam_data:/data
    restart: unless-stopped

volumes:
  oscam_data:
    name: oscam_data
