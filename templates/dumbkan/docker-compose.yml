version: '3.8'

services:
  dumbkan:
    image: docker.io/dumbwareio/dumbkan:latest
    container_name: dumbkan
    hostname: dumbkan
    ports:
      - "${DUMBKAN_PORT:-8080}:8080"
    volumes:
      - dumbkan_data:/data
    restart: unless-stopped

volumes:
  dumbkan_data:
    name: dumbkan_data
