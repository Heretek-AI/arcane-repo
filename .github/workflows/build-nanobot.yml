name: Build NanoBot

on:
  push:
    paths:
      - 'scripts/dockerfiles/nanobot/**'
      - 'templates/nanobot/**'
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push NanoBot
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/nanobot:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/nanobot/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
