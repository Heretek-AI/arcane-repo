version: '3.8'

services:
  hydra2:
    image: docker.io/linuxserver/hydra2:latest
    container_name: hydra2
    hostname: hydra2
    ports:
      - "${HYDRA2_PORT:-8080}:8080"
    volumes:
      - hydra2_data:/data
    restart: unless-stopped

volumes:
  hydra2_data:
    name: hydra2_data
