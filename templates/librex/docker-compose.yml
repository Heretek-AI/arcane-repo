version: '3.8'

services:
  librex:
    image: docker.io/librex/librex:latest
    container_name: librex
    hostname: librex
    ports:
      - "${LIBREX_PORT:-8080}:8080"
    volumes:
      - librex_data:/data
    restart: unless-stopped

volumes:
  librex_data:
    name: librex_data
