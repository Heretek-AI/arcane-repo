version: '3.8'

services:
  hauk:
    image: docker.io/bilde2910/hauk:latest
    container_name: hauk
    hostname: hauk
    ports:
      - "${HAUK_PORT:-8080}:8080"
    volumes:
      - hauk_data:/data
    restart: unless-stopped

volumes:
  hauk_data:
    name: hauk_data
