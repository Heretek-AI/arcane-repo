version: '3.8'

services:
  mopidy:
    image: docker.io/wernight/mopidy:latest
    container_name: mopidy
    hostname: mopidy
    ports:
      - "${MOPIDY_PORT:-8080}:8080"
    volumes:
      - mopidy_data:/data
    restart: unless-stopped

volumes:
  mopidy_data:
    name: mopidy_data
