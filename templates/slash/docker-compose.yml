version: '3.8'

services:
  slash:
    image: docker.io/yourselfhosted/slash:latest
    container_name: slash
    hostname: slash
    ports:
      - "${SLASH_PORT:-8080}:8080"
    volumes:
      - slash_data:/data
    restart: unless-stopped

volumes:
  slash_data:
    name: slash_data
