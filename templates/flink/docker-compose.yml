version: '3.8'

services:
  flink:
    image: docker.io/apache/flink:latest
    container_name: flink
    hostname: flink
    ports:
      - "${FLINK_PORT:-8080}:8080"
    volumes:
      - flink_data:/data
    restart: unless-stopped

volumes:
  flink_data:
    name: flink_data
