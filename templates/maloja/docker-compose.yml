version: '3.8'

services:
  maloja:
    image: docker.io/krateng/maloja:latest
    container_name: maloja
    hostname: maloja
    ports:
      - "${MALOJA_PORT:-8080}:8080"
    volumes:
      - maloja_data:/data
    restart: unless-stopped

volumes:
  maloja_data:
    name: maloja_data
