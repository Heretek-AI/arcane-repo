version: '3.8'

services:
  upsnap:
    image: ghcr.io/seriousm4x/upsnap:latest
    container_name: upsnap
    hostname: upsnap
    ports:
      - "${UPSNAP_PORT:-8080}:8080"
    volumes:
      - upsnap_data:/data
    restart: unless-stopped

volumes:
  upsnap_data:
    name: upsnap_data
