version: '3.8'

services:
  wikidocs:
    image: docker.io/zavy86/wikidocs:latest
    container_name: wikidocs
    hostname: wikidocs
    ports:
      - "${WIKIDOCS_PORT:-80}:80"
    volumes:
      - wikidocs_data:/data
    restart: unless-stopped

volumes:
  wikidocs_data:
    name: wikidocs_data
