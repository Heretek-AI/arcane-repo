version: '3.8'

services:
  sharkey:
    image: docker.io/juneink/sharkey:latest
    container_name: sharkey
    hostname: sharkey
    ports:
      - "${SHARKEY_PORT:-8080}:8080"
    volumes:
      - sharkey_data:/data
    restart: unless-stopped

volumes:
  sharkey_data:
    name: sharkey_data
