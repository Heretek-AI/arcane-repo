version: '3.8'

services:
  esphome:
    image: ghcr.io/esphome/esphome:latest
    container_name: esphome
    hostname: esphome
    ports:
      - "${ESPHOME_PORT:-8080}:8080"
    volumes:
      - esphome_data:/data
    restart: unless-stopped

volumes:
  esphome_data:
    name: esphome_data
