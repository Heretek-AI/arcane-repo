# ── Puck Configuration ──────────────────────────────────────────
# Copy this file to .env and adjust values as needed.

# Puck is an AI agent orchestration tool. This Docker template provides
# a minimal informational API wrapper. For full functionality, install
# puck via pip directly in your project:
#   pip install puck
# See README.md for details.

# Puck API port (default: 8000)
PUCK_PORT=8000
