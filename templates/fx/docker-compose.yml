version: '3.8'

services:
  fx:
    image: docker.io/rikhuijzer/fx:latest
    container_name: fx
    hostname: fx
    ports:
      - "${FX_PORT:-8080}:8080"
    volumes:
      - fx_data:/data
    restart: unless-stopped

volumes:
  fx_data:
    name: fx_data
