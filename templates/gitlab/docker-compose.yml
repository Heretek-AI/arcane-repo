version: '3.8'

services:
  gitlab:
    image: docker.io/mcp/gitlab:latest
    container_name: gitlab
    hostname: gitlab
    ports:
      - "${GITLAB_PORT:-80}:80"
    volumes:
      - gitlab_data:/data
    restart: unless-stopped

volumes:
  gitlab_data:
    name: gitlab_data
