version: '3.8'

services:
  scratch:
    image: docker.io/library/scratch:latest
    container_name: scratch
    hostname: scratch
    ports:
      - "${SCRATCH_PORT:-8080}:8080"
    volumes:
      - scratch_data:/data
    restart: unless-stopped

volumes:
  scratch_data:
    name: scratch_data
