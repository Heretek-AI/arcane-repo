version: "3.8"

services:
  plexe:
    image: plexe/plexe:latest
    container_name: plexe
    hostname: plexe
    ports:
      - "${PLEXE_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - PLEXE_PORT=${PLEXE_PORT:-8080}

