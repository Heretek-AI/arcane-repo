name: Build Plexe

on:
  push:
    paths:
      - 'scripts/dockerfiles/plexe/**'
      - 'templates/plexe/**'
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Plexe
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/plexe:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/plexe/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
