# =============================================================================
# Uncloud — Environment Configuration
# Lightweight container orchestration across Docker hosts with WireGuard mesh
# =============================================================================

# Port exposed by the FastAPI management wrapper (default: 8000)
UNCLOUD_PORT=8000
