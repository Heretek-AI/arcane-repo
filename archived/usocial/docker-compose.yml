version: '3.8'

services:
  usocial:
    image: docker.io/rhizomik/usocial:latest
    container_name: usocial
    hostname: usocial
    ports:
      - "${USOCIAL_PORT:-8080}:8080"
    volumes:
      - usocial_data:/data
    restart: unless-stopped

volumes:
  usocial_data:
    name: usocial_data
