version: '3.8'

services:
  petio:
    image: docker.io/bricksoft/petio:latest
    container_name: petio
    hostname: petio
    ports:
      - "${PETIO_PORT:-8080}:8080"
    volumes:
      - petio_data:/data
    restart: unless-stopped

volumes:
  petio_data:
    name: petio_data
