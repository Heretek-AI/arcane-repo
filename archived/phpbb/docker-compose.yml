version: '3.8'

services:
  phpbb:
    image: docker.io/vimagick/phpbb:latest
    container_name: phpbb
    hostname: phpbb
    ports:
      - "${PHPBB_PORT:-80}:80"
    volumes:
      - phpbb_data:/data
    restart: unless-stopped

volumes:
  phpbb_data:
    name: phpbb_data
