version: "3.8"

services:
  openspiel:
    image: ghcr.io/arcane-repo/openspiel:latest
    container_name: openspiel
    hostname: openspiel
    ports:
      - "${OPENSPIEL_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - OPENSPIEL_PORT=${OPENSPIEL_PORT:-8080}

