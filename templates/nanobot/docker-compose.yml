version: "3.8"

services:
  nanobot:
    image: hkunlp/nanobot:latest
    container_name: nanobot
    hostname: nanobot
    ports:
      - "${NANOBOT_PORT:-8000}:8000"
    restart: unless-stopped
    environment:
      - NANOBOT_PORT=${NANOBOT_PORT:-8000}

