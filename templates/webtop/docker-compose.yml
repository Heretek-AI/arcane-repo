version: '3.8'

services:
  webtop:
    image: ghcr.io/linuxserver/webtop:latest
    container_name: webtop
    hostname: webtop
    ports:
      - "${WEBTOP_PORT:-8080}:8080"
    volumes:
      - webtop_data:/data
    restart: unless-stopped

volumes:
  webtop_data:
    name: webtop_data
