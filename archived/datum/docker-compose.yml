version: '3.8'

services:
  datum:
    image: docker.io/makkambaevdastan/datum:latest
    container_name: datum
    hostname: datum
    ports:
      - "${DATUM_PORT:-8080}:8080"
    volumes:
      - datum_data:/data
    restart: unless-stopped

volumes:
  datum_data:
    name: datum_data
