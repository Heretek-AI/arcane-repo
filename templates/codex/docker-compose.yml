version: '3.8'

services:
  codex:
    image: ghcr.io/ajslater/codex:latest
    container_name: codex
    hostname: codex
    ports:
      - "${CODEX_PORT:-8080}:8080"
    volumes:
      - codex_data:/data
    restart: unless-stopped

volumes:
  codex_data:
    name: codex_data
