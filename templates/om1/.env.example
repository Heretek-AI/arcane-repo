# Host port for the OM1 FastAPI wrapper (headless mode)
OM1_PORT=8000

# Run in headless mode (no hardware devices required)
OM1_HEADLESS=true

# Skip internet connectivity check on startup
OM1_SKIP_INTERNET_CHECK=true
