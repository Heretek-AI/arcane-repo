name: Build CC Gateway

on:
  push:
    paths:
      - 'scripts/dockerfiles/cc-gateway/**'
      - 'templates/cc-gateway/**'
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push CC Gateway
        run: |
          IMAGE="ghcr.io/heretek-ai/arcane-repo/cc-gateway:latest"
          echo "Building $IMAGE..."
          docker build -t "$IMAGE" -f scripts/dockerfiles/cc-gateway/Dockerfile .
          docker push "$IMAGE"
          echo "Published: $IMAGE"
