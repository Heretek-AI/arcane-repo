version: "3.8"

services:
  solacemesh:
    image: ghcr.io/solacelabs/solace-agent-mesh:1.7.5
    container_name: solacemesh
    hostname: solacemesh
    ports:
      - "${SOLACE AGENT MESH_PORT:-8080}:8080"
    restart: unless-stopped
    environment:
      - SOLACE_PORT=${SOLACE_PORT:-8080}

